# 项目约束

> 本文件是本项目(工作目录 `/Users/azm/MyProject/work`)的项目级约束,所有在此目录内工作的助手与协作者都必须遵守。
> 全局约束见 `~/.codex/AGENTS.md`(中文交流、隐私保护、文档统一放 `docs/`)。

## 一、引用规范:指明"文档—Sheet—字段"时必须使用完整名称(强制)

在文档、报告、说明书、注释、交付物与对外消息中,**凡是指明数据来源或依据,必须使用完整且与来源一致的名称**;
不得使用简称、缩写、别名、自造名称,也不得出现层级错位(例如把 Sheet 名当文件名)。

### 1. 文档:完整文件名 + 扩展名,并用书名号包裹

- `《2026H1渠道销售明细.xlsx》`
- `《2026H1门店经营台账.xlsx》`
- `《2026H1线上投放与平台费用.xlsx》`
- `《2026H1会员与跨渠道订单.xlsx》`
- `《2026H1促销活动记录.xlsx》`
- `《华东区门店租约汇总.docx》`
- `《渠道与门店管理策略.docx》`
- `《SKU主数据.xlsx》`
- `《旧版门店清单_2024.xlsx》`

交付物同样必须写全名 + 扩展名:`《2026H2 渠道与门店经营策略报告.docx》`、`《门店分级与调整建议.xlsx》`、
`《渠道资源再配置建议.xlsx》`、`《促销组合优化建议.xlsx》`、`《异常与待核清单.xlsx》`、`《数据血缘说明.md》`。

### 2. Sheet:必须使用原始 Sheet 名,并显式标注"Sheet"

- 正确:`《2026H1渠道销售明细.xlsx》渠道销售明细 Sheet`、`《2026H1门店经营台账.xlsx》门店费用 Sheet`、`《2026H1促销活动记录.xlsx》活动效果 Sheet`
- 错误:把 Sheet 名当文件名——写 `《门店费用》`、`《渠道月度汇总》`、`《会员订单》`
- 错误:只写"门店费用 Sheet""活动效果 Sheet",不指明属于哪个文件

### 3. 字段:必须使用原始字段名

- 用反引号包裹原始字段名,例如 `` `净收入` ``、`` `门店O2O履约贡献` ``、`` `租金及物业费` ``、`` `面积是否调整` ``、`` `线上订单履约单量` ``
- 不得改写字段名(如把 `门店O2O履约贡献` 写成"O2O 贡献""履约收入")
- 不得省略字段所属的 Sheet

### 4. 章节与条款:写全 `《文件名》第 X 条`

- 正确:`《渠道与门店管理策略.docx》第 2.5 条`
- 错误:`策略2.5条`、`制度 6.1 条`(未指明文件名)

### 5. 禁止使用的简称(示例,不限于此)

| 禁止写法 | 规范写法 |
| --- | --- |
| 台账、门店台账 | `《2026H1门店经营台账.xlsx》` |
| 渠道明细 | `《2026H1渠道销售明细.xlsx》` |
| 租约、租约文档 | `《华东区门店租约汇总.docx》` |
| 策略文档、策略、制度 | `《渠道与门店管理策略.docx》` |
| 活动表、促销表 | `《2026H1促销活动记录.xlsx》` |
| 会员表、会员数据 | `《2026H1会员与跨渠道订单.xlsx》` |
| SKU 表 | `《SKU主数据.xlsx》` |
| 旧版清单 | `《旧版门店清单_2024.xlsx》` |

### 6. 脚本或代码中引用时,同时给出路径

例如:`input/2026H1门店经营台账.xlsx` → Sheet「门店费用」→ 字段 `租金及物业费`。

### 7. 唯一例外:逐字引用

引用模型产物、外部材料或历史文档的**原文**时,允许照录其中的简称;但必须在引用处注明"引文照录",
必要时用括号补出规范写法。

## 二、执行要求

1. 新增或修改文档、交付物、脚本前,先按上述规范自检引用名称;
2. 机审脚本 `docs/skill/validate_all.py` 已加入"书名号引用必须含扩展名"的检查,**提交前必须通过**;
   检查会跳过反例行(`❌`)与"引文照录"行;
3. 发现历史文档存在简称、别名或层级错位时,应逐步修正为完整名称;
4. 交付物(报告、Excel、说明书)中的"关键依据""披露依据"等回指字段同样适用本规范。

## 三、目录约定:工具箱与过程文档分开存放(强制)

**可跨题复用的出题工具**(培训文档、工作流 SOP、写作规则、机审与自检脚本)统一放在 `docs/skill/`,
不得与某一道题的过程文档混放。详见 `docs/skill/README.md`。

| 目录 | 放什么 | 复用性 |
| --- | --- | --- |
| `docs/skill/` | `rules1.md`、`rules2.md`、`出题工作流.md`、`step3-writing-guide.md`、`口径登记.md`、`data-validation.py`、`validate_a_module_levels.py`、`validate_lineage.py`、`validate_baseline.py`、`validate_calibers.py`、`validate_all.py`、`baseline_snapshot.json` | **可复用**,下道题直接沿用 |
| `docs/specs/` | 本题产物:任务说明、Query、评估表 | 每题重写 |
| `docs/reviews/` | 本题复盘:材料问题清单、各模型结果评估 | 每题重写 |
| `input/`、`output/`、`m1~m3-deliverables/` | 本题材料与各产物 | 每题重写 |
| 项目根目录 | `num.py`、`build_solution.py` 等含本题业务逻辑的生成脚本 | 骨架复用、业务逻辑重写 |

执行要求:

1. 新增可复用工具时放进 `docs/skill/`,并同步更新 `docs/skill/出题工作流.md` 与 `docs/skill/README.md`;
2. `docs/skill/rules1.md`、`rules2.md` 是平台培训文档原文,**只读不改**;版本更新时整体替换并记录版本号;
3. 脚本移动目录后,须确认 `docs/skill/validate_all.py` 中的 `SPECS` / `SKILL` 常量指向正确,
   并全仓扫描一遍过期路径引用。

## 四、材料生成约束:汇总必须由明细派生(强制)

适用于一切由生成器产出的附件。约束编号 C-1~C-5:

1. **C-1 汇总必须由明细派生**:凡题包内同时存在某业务的明细表与汇总表,汇总值必须由明细表按
   "主键 + 期间"聚合得出,**禁止独立随机生成**;
2. **C-2 无明细时允许直接生成,但必须登记并声明**:某指标确无明细来源时(如人力费用、水电、
   折旧摊销、到店销售额、履约费),可直接生成汇总值,但必须:① 在 `docs/skill/data-validation.py`
   的汇总矩阵中登记为"原始汇总值";② 在制度或《异常与待核清单.xlsx》中声明其"只到汇总级、
   不可独立复算";③ 不以其作为需要独立验证的核心结论的唯一依据;
3. **C-3 汇总字段必须登记来源类别**:① 明细聚合 / ② 行内派生 / ③ 原始汇总值,三类必居其一,
   由 R-11 检查;③ 类是否已声明,由 R-12 检查;
4. **C-4 判定方法**:删掉明细表后,该汇总值仍能被解释 → ① 或 ②;不能被解释 → ③,必须声明。
5. **C-5 改动必须做基线回归**:每改一次生成器,都要与上一版材料逐 Sheet 比对,确认
   "**实际变化 ⊆ 预期变化**"。执行方式:
   `.venv/bin/python docs/skill/validate_baseline.py --expect "预期变化的文件名"`
   —— 出现超出预期的变化即失败(退出码 1);确认无误后再运行 `--update` 更新基线快照。
   背景:本项目曾因调整生成逻辑时无意打乱随机数调用顺序,导致 9 个附件全量漂移,
   当时只能靠人工逐格比对发现。

执行要求:

1. 新增或修改任何汇总 Sheet 的数值字段后,必须同步登记到 `docs/skill/data-validation.py` 的
   `AGG_REGISTRY`,再跑一次自检;
2. 自检报告中 R-11 报"未登记"、R-12 报"未声明"时,须在材料或制度中补齐后方可提交;
3. 该约束同时适用于交付物:凡引用汇总值的结论,应能说明其来源类别。
4. `docs/skill/validate_all.py` 的第 6 项"材料基线比对"只作报告、不阻断;
   若报告出变化,须确认是否在预期内,并在变更记录中写明本次改了哪些附件与 Sheet;
5. 涉及企业口径的新增或修改,必须:先改《渠道与门店管理策略.docx》→ 再登记到
   `docs/skill/口径登记.md` → 最后运行 `docs/skill/validate_calibers.py` 确认
   制度、Query、评估表三处锚点齐全(或明确标注"—"并写明理由)。
